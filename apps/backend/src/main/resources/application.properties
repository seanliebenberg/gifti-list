spring.application.name=backend
server.port=8080
# silence the Compose helper until I actually need docker compose
spring.docker.compose.enabled=false
spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest